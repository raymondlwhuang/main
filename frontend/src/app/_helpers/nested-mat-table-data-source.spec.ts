import { NestedMatTableDataSource } from './nested-mat-table-data-source';

describe('NestedMatTableDataSource', () => {
  it('should create an instance', () => {
    expect(new NestedMatTableDataSource()).toBeTruthy();
  });
});
